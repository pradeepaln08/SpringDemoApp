name: Java CI with Maven

on:
  push:
    branches: ["main"]
  
jobs:
  build:
    runs-on: winrun01

    steps:
      - name: PWD
        run: pwd
        
      - name: Go to path
        run: | 
              cd target/
              ls -l
        
      - name: PWD
        run: pwd
    
